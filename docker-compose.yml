version: '3.8'

services:
  # Backend API
  backend:
    build:
      context: ./kangarubackend
      dockerfile: Dockerfile
    container_name: kangarubackend
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGINS=http://localhost:3000
      - PUBLIC_ORIGIN=http://localhost:4000
    volumes:
      # Persist uploads (optional for local dev)
      - ./kangarubackend/public/uploads:/app/public/uploads
      - ./kangarubackend/downloads:/app/downloads
    networks:
      - kangarunetwork
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:4000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend React App
  frontend:
    build:
      context: ./kangarufrontend
      dockerfile: Dockerfile
    container_name: kangarufrontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - VITE_API_URL=http://localhost:4000
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - kangarunetwork
    restart: unless-stopped

networks:
  kangarunetwork:
    driver: bridge

# Usage:
# 1. Create .env file with MONGO_URI and JWT_SECRET
# 2. Run: docker-compose up --build
# 3. Access frontend at http://localhost:3000
# 4. Access backend API at http://localhost:4000
